# PBN 링크 빌딩 시스템 추가 의존성
# 기존 requirements.txt에 추가로 설치해야 할 패키지들

# 텍스트 유사도 계산을 위한 라이브러리
sentence-transformers>=2.2.2
scikit-learn>=1.3.0
numpy>=1.24.0

# 웹 크롤링 및 HTTP 요청
requests>=2.31.0
urllib3>=2.0.0

# 데이터베이스 (SQLite는 Python 내장)
# sqlite3는 Python 표준 라이브러리에 포함됨

# 병렬 처리 (concurrent.futures는 Python 내장)
# threading도 Python 표준 라이브러리에 포함됨

# 데이터 클래스 (dataclasses는 Python 3.7+ 내장)
# typing도 Python 표준 라이브러리에 포함됨

# 정규표현식 (re는 Python 내장)
# json도 Python 표준 라이브러리에 포함됨

# 시간 관련 (time, datetime은 Python 내장)
# pathlib도 Python 3.4+ 표준 라이브러리에 포함됨

# 기존 의존성들은 requirements.txt에서 확인
# 추가로 필요한 패키지만 여기에 나열됨
